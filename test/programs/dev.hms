# This file is used for implementing new language features
let object = {
    key: 42,
};

object.foo = "bar";
debug(object);

let letters = "a,b,c,d,e,f".split(",");
println(letters);
println(letters.contains("a"));

println("hello world!".contains("z"));
println("hello world!".contains("hello "));

println("Stringlength:", "foo-bar".len());

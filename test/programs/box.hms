fn box(dimensions) {
    let output = "";
    for y in dimensions.range() {
        for x in (dimensions*2).range() {
            if x == 0 || y == 0 || x == dimensions*2-1 || y == dimensions-1 {
                output += "#";
            } else {
                output += " ";
            };
        };
        output += "\n";
    };
    return output;
};

for size in 2..8 {
    println(box(size));
};

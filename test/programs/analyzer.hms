fn box(dimensions) {
    for y in 0..dimensions {
        for x in 0..dimensions {
            if x == 0 || y == 0 || x == dimensions-1 || y == dimensions-1 {
                print("#");
            } else {
                print(" ");
            };
        };
    };
    print("\n");
};

box(30);
